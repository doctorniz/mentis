# Helps some tools (including Turbopack on Windows) resolve `next` when using pnpm’s isolated layout.
# If `pnpm dev` ever fails with "Next.js package not found", run `pnpm install` again, or use `pnpm dev:webpack`.
public-hoist-pattern[]=*next*
