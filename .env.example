# Copy to `.env.local` and fill in. Restart `pnpm dev` after changes.
# See docs/CLOUD_SYNC.md for Dropbox setup.

# Dropbox App Console → App key (OAuth client ID). Safe to expose in the frontend.
# NEXT_PUBLIC_DROPBOX_CLIENT_ID=
